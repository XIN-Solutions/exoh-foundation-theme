# Hero Image (stub)

Per-section MCP guidance for the Hero Image component (`partials/sections/hero/hero/`). Placeholder from theme MCP enablement.

Full prose (purpose, config fields, anti-patterns, examples) will be added in `mcp-component-md-decoration`.
