# Theme MCP guidance (stub)

Theme-wide agent guidance for the EX:OH Foundation Theme. This file is a placeholder from theme MCP enablement.

Full prose (page templates, recipes, cross-cutting rules) will be authored in `mcp-theme-md-decoration`. Per-section explainers live under `partials/sections/**/.mcp.md` (see `mcp-component-md-decoration`).

Conventions: `metadata.json` `mcpEnabled`, theme-root `LLM.md`, and `partials/sections/**/.mcp.md`. See project-context `docs/mcp/theme-conventions.md`.
